// Math = build-in object that provides a collection of properties and methods

let x = 3
let y = 2
let z = 1

//z = Math.round(x) round the number up or down
//z = Math.floor(x) round always to the lower number
//z = Math.ceil(x)  round always to the upper number
//z = Math.trunc(x)
//z = Math.pow(x, y) "x to the POWer of y"
//z = Math.sqrt(x)
//z = Math.log(x) its always the natural log of x
//z = Math.sin(x) always the radiants
//z = Math.cos(x) 
//z = Math.tan(x)
//z = Math.abs(x) the absolute number (the same x but positive)
//z = Math.sign(x) returns the sign of the number (positive, negative or zero)
//let max = Math.max(x, y, z)
let min = Math.min(x, y, z)

console.log(min)