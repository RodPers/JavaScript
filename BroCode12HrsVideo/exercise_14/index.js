// strings methods = allow you to manipulate and work with text (strings)

let username = 'BroCode'

//username.charAt(0) //return the letter selected 
//console.log(username.charAt())

//console.log(username.indexOf("B")) //return the first occurrence of the letter

//console.log(username.lastIndexOf("o")) //return the last occurrence of the letter

//console.log(username.length) //shows the length of the string

//console.log(username.trim()) //removes the white spaces before and after the string

//console.log(username.toUpperCase()) //uppercase the string letters

//console.log(username.toLowerCase()) //lowercase the string letters

//console.log(username.repeat(2)) //repeats the string x times 

//console.log(username.startsWith('B')) //returns if's true or false the parameter

//console.log(username.endsWith('o')) //same but different

//console.log(username.includes()) //set something to check if string includes the parameter
/*
let phoneNumber = "123-456-7890"

phoneNumber = phoneNumber.replaceAll("-", "")

console.log(phoneNumber)

here we are replacing the character "-" of our string to nothing, to it became a number - still a string, but with no hyphen */

//console.log(username.padStart(15, "0")) //set the lenght of our string to the number specified, and then complete, if necessary, the string with the character 

//console.log(username.padEnd(15, "0")) //same but different 